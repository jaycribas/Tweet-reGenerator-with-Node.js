extends layout

block content
    nav
      a.nav-link(
        href='/'
      ) Back to Home

    main
      table.tweets.datatable
        thead
          tr
            th saved tweets
        tbody
          for twit in tweets
            tr
              td= twit.tweet
                form(
                  action='/retweet/' + twit.id_str
                  method='post'
                )
                  button.retweet(
                    type='submit'
                  )
                    i.fa.fa-retweet

    script.
      $('.tweets').DataTable( {
      'iDisplayLength': 50
      });
      $('.dataTables_filter input').attr("placeholder", "search");
